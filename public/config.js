// Vlogify frontend configuration
// When deployed as a single app (Render/Docker/local), API_BASE is empty (same-origin)
// When frontend is hosted separately, set API_BASE to the backend URL
window.VLOGIFY_API_BASE = '';
